{
  "name": "fullstack-social-app",
  "version": "1.0.0",
  "description": "fullstack-social-app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-start": "cd client && npm start",
    "client-build": "cd client && npm run build",
    "dev": "nodemon server/server.js",
    "start": "node server/server.js",
    "start-dev-stack": "concurrently \"npm run dev\" \"npm run client-start\"",
    "setup-logs": "mkdir server/logs"
  },
  "author": "Mohamed Hafez",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.2"
  }
}
